# JobPilot — Tailwind v3 Ready

- This package pins Tailwind to v3 and includes a CRA/Webpack-friendly PostCSS config.
- CSS directives ensured in: `src/index.css`
- CSS import added in one of: index.tsx/jsx or main.tsx/jsx.

## Run
cd jobpilot/frontend/
rm -rf node_modules package-lock.json
npm install
npm start
